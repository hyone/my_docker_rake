require 'my_docker_rake/tasks'

MyDockerRake::Tasks.new do |c|
  c.image          = 'mydockerrake/sshd:v2'
  c.container      = 'mydockerrake.sshd'
  c.data_image     = 'mydockerrake/data'
  c.data_container = 'mydockerrake.data'
  c.run_options    = '-p 22'
end
